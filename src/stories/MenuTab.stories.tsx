import MenuTab from '@components/compound/MenuTab';
import type { ReactElement } from 'react';

export default {
  title: 'Component/compound',
  component: MenuTab
};

export const Default = (): ReactElement => {
  return <MenuTab></MenuTab>;
};
