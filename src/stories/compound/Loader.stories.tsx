import Loader from '@compound/Loader';
import type { ReactElement } from 'react';

export default {
  title: 'Component/Compound/Loader',
  component: Loader
};

export const Default = (): ReactElement => <Loader />;
